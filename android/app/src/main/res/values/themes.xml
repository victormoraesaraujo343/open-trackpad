<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      v0.1 is a debugging surface, not the final look. The themes in the UI spec
      come in v0.2, once the native trackpad is proven on hardware.
    -->
    <style name="Theme.OpenTrackpad" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@color/surface_background</item>
        <item name="android:statusBarColor">@color/surface_background</item>
        <item name="android:navigationBarColor">@color/surface_background</item>
    </style>
</resources>
