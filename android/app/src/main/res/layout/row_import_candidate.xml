<?xml version="1.0" encoding="utf-8"?>
<!--
  One shortcut being offered: a tick, what it does, and the keys it uses.

  The whole row is the target rather than the box alone. A 15dp checkbox is a
  cruel thing to aim at, and there is nothing else in the row to press.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/row"
    android:clickable="true"
    android:focusable="true"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingHorizontal="8dp"
    android:paddingVertical="6dp">

    <FrameLayout
        android:id="@+id/candidate_box"
        android:layout_width="15dp"
        android:layout_height="15dp"
        android:background="@drawable/tick_box"
        android:duplicateParentState="true">

        <ImageView
            android:id="@+id/candidate_tick"
            android:layout_width="10dp"
            android:layout_height="10dp"
            android:layout_gravity="center"
            android:importantForAccessibility="no"
            android:src="@drawable/ic_tick" />
    </FrameLayout>

    <TextView
        android:id="@+id/candidate_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="10dp"
        android:layout_weight="1"
        android:ellipsize="end"
        android:fontFamily="@font/inter_medium"
        android:singleLine="true"
        android:textColor="@color/secondary"
        android:textSize="11sp"
        tools:text="Show all windows" />

    <TextView
        android:id="@+id/candidate_chord"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="10dp"
        android:background="@drawable/chord_chip"
        android:fontFamily="@font/inter_regular"
        android:paddingHorizontal="8dp"
        android:paddingVertical="3dp"
        android:textColor="@color/secondary"
        android:textSize="10sp"
        tools:text="super+w" />
</LinearLayout>
