<?xml version="1.0" encoding="utf-8"?>
<!--
  One shortcut in the picker: a glyph, its name, and the chord under it.

  The glyph is not decoration. The rails draw glyphs and nothing else
  identifies a slot at a glance, so this is where somebody learns which symbol
  means which shortcut — without it you drag a name onto a rail and meet a
  picture you have never seen.

  The dot marks one somebody recorded themselves, which is the only kind that
  can be renamed or deleted.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:background="@drawable/chip">

    <ImageView
        android:id="@+id/chip_icon"
        android:layout_width="14dp"
        android:layout_height="14dp"
        android:importantForAccessibility="no" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/chip_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:singleLine="true"
            android:ellipsize="end"
            android:fontFamily="@font/inter_medium"
            android:textColor="?attr/otpSecondary"
            android:tag="13"
            android:textSize="13sp" />

        <TextView
            android:id="@+id/chip_chord"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:singleLine="true"
            android:ellipsize="end"
            android:fontFamily="@font/inter_regular"
            android:textColor="?attr/otpMuted"
            android:tag="12"
            android:textSize="12sp" />
    </LinearLayout>

    <View
        android:id="@+id/chip_mine"
        android:layout_width="5dp"
        android:layout_height="5dp"
        android:background="@drawable/mine_dot" />
</LinearLayout>
